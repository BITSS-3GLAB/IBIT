<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/common_bg"
    android:orientation="vertical" >

    <RelativeLayout
        android:id="@+id/bus_top_bar"
        android:layout_width="fill_parent"
        android:layout_height="48dp"
        android:background="#31C5AB" >

        <ImageView
            android:id="@+id/busplacechoose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentLeft="true"
            android:layout_centerInParent="true"
            android:layout_marginLeft="8dp"
            android:scaleType="center"
            android:src="@drawable/bg_change_place" >
        </ImageView>

        <TextView
            android:id="@+id/bustimeshow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginTop="5dp"
            android:gravity="center"
            android:text="2012年06月28日 周四"
            android:textColor="#ffffff"
            android:textSize="16dp" />

        <ImageView
            android:id="@+id/bustimechoose"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentRight="true"
            android:layout_centerInParent="true"
            android:layout_marginRight="8.0dp"
            android:scaleType="center"
            android:src="@drawable/bg_change_time" >
        </ImageView>
    </RelativeLayout>

    <ListView
        android:id="@+id/busdatalistview"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/bus_top_bar"
        android:cacheColorHint="#00000000"
        android:divider="#00000000"
        android:dividerHeight="0.7dp"
        android:fadingEdge="none" >
    </ListView>

</RelativeLayout>